trigger_pipeline(
        'clct_tag_new_mt',
        variables={},
        check_status=False,
        error_on_failure=False,
        poll_interval=60,
        poll_timeout=None,
        schedule_name='clct_tag_new_mt',  # Enter a unique name to create a new trigger each time
        verbose=True,
    )